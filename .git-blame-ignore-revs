# Commits listed here are skipped by `git blame` so that large, mechanical
# changes (bulk reformatting, renames) don't obscure the real authorship of
# each line.
#
# GitHub's web blame view uses this file automatically. For local CLI blame,
# opt in once per clone:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs

# style: apply ruff format across the repository
fd8fc4064887d58383277db125c0df00a148ce2a

"""Stage 0 regression harness for CPU / AMD ROCm accelerator support.

See ``cpu_rocm_support_plan.md`` (Stage 0): capture today's baseline
behavior on CPU-only hardware -- analyze/preprocess already work; mist_train
cannot even be imported without ``nvidia-dali-cuda120`` installed -- so later
stages have something concrete to diff their progress against.
"""
